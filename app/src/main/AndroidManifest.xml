<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalizacaoSimples"
        tools:targetApi="31">

        <!-- LoginActivity é a tela inicial -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de Cadastro -->
        <activity
            android:name=".CadastroActivity"
            android:exported="false" />

        <!-- Tela Principal (rastreamento) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Tela de Gerenciar Compartilhamento -->
        <activity
            android:name=".ManageSharingActivity"
            android:label="Gerenciar Compartilhamento"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

        <!-- Tela de Visualizar Localizações -->
        <activity
            android:name=".SharedLocationsActivity"
            android:label="Localizações Compartilhadas"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

    </application>

</manifest>